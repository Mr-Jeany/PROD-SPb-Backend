# 🚀 Быстрый старт PRODOne API

## 📋 Что нужно сделать

### 1. Установить Python зависимости
```bash
cd PRODOne
pip install -r requirements.txt
```

### 2. Запустить сервер
```bash
python run_server.py
```

Сервер запустится на `http://localhost:5000`

### 3. Протестировать API
```bash
python test_api.py
```

### 4. Запустить мобильное приложение
В Android Studio запустите приложение - оно автоматически подключится к API.

## 🔑 Тестовые данные

После запуска `run_server.py` будут созданы:

**Пользователь:**
- Телефон: `+7 900 123 45 67`
- Email: `test@example.com`
- Пароль: `password123`

**Товары:**
- 20+ товаров в 8 категориях
- Поиск по названию, описанию, тегам
- Пагинация (по 6 товаров)
- Фильтрация по категориям

## 📱 Настройка мобильного приложения

URL уже настроен в `ApiClient.kt`:
```kotlin
private const val BASE_URL = "http://10.0.2.2:5000/api/"
```

Для реального устройства замените на IP адрес вашего компьютера.

## 🧪 Тестирование

### Проверка API в браузере:
- http://localhost:5000/api/health
- http://localhost:5000/api/categories
- http://localhost:5000/api/items/search?name=молоко

### Тестирование с curl:
```bash
# Поиск товаров
curl "http://localhost:5000/api/items/search?name=молоко"

# Получение категорий
curl http://localhost:5000/api/categories

# Вход пользователя
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phoneOrEmail": "+7 900 123 45 67", "password": "password123"}'
```

## 🐛 Решение проблем

### Сервер не запускается
- Проверьте, что порт 5000 свободен
- Убедитесь, что установлены все зависимости

### Мобильное приложение не подключается
- Проверьте, что сервер запущен
- Для эмулятора используйте `10.0.2.2:5000`
- Для реального устройства используйте IP компьютера

### Ошибки базы данных
- Удалите `main.db` и перезапустите сервер
- Сервер автоматически создаст новую базу с тестовыми данными

## 📊 Структура проекта

```
PRODOne/
├── app.py              # Основной API сервер
├── db_types.py         # Модели базы данных
├── creating_tables.py  # Создание таблиц
├── seed_data.py        # Заполнение тестовыми данными
├── run_server.py       # Скрипт запуска сервера
├── test_api.py         # Тестирование API
├── requirements.txt    # Python зависимости
├── README.md          # Подробная документация
└── QUICK_START.md     # Этот файл
```

## 🎯 Готово!

После выполнения этих шагов у вас будет:
- ✅ Рабочий API сервер
- ✅ База данных с тестовыми данными
- ✅ Все необходимые эндпоинты
- ✅ Система аутентификации
- ✅ Мобильное приложение, подключенное к API

**Приятной разработки! 🚀**
