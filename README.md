# 🛒 PRODOne API Backend

Backend API для мобильного приложения заказа продуктов.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск сервера

```bash
python run_server.py
```

Сервер будет доступен по адресу: `http://localhost:5000`

## 📋 API Endpoints

### Товары

#### Поиск товаров
```
GET /api/items/search?name={query}
```
**Параметры:**
- `name` (обязательный) - поисковый запрос

**Ответ:** Массив товаров в формате JSON

#### Получение товаров с пагинацией
```
GET /api/items/scroll?offset={offset}&limit={limit}
```
**Параметры:**
- `offset` (опциональный, по умолчанию 0) - смещение
- `limit` (опциональный, по умолчанию 6) - количество товаров

**Ответ:** Массив товаров в формате JSON

#### Товары по категории
```
GET /api/items/category?categoryId={id}
GET /api/items/category?categoryName={name}
```
**Параметры:**
- `categoryId` (опциональный) - ID категории
- `categoryName` (опциональный) - название категории

**Ответ:** Массив товаров в формате JSON

### Категории

#### Получение всех категорий
```
GET /api/categories
```
**Ответ:** Массив категорий в формате JSON

### Аутентификация

#### Регистрация пользователя
```
POST /api/auth/register
```
**Тело запроса:**
```json
{
  "name": "Иван Иванов",
  "phoneOrEmail": "+7 900 123 45 67",
  "password": "password123"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Пользователь успешно зарегистрирован",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

#### Вход пользователя
```
POST /api/auth/login
```
**Тело запроса:**
```json
{
  "phoneOrEmail": "+7 900 123 45 67",
  "password": "password123"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Успешная авторизация",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

#### Профиль пользователя
```
GET /api/user/profile
```
**Заголовки:**
- `Authorization: Bearer {token}`

**Ответ:**
```json
{
  "id": 1,
  "name": "Иван Иванов",
  "phone": "+7 900 123 45 67",
  "email": "test@example.com",
  "address": "Москва, ул. Тестовая, д. 1",
  "avatarUrl": null
}
```

### Проверка здоровья
```
GET /api/health
```
**Ответ:**
```json
{
  "status": "OK",
  "message": "API is running"
}
```

## 🗄️ База данных

### Структура таблиц

#### Shops (Магазины)
- `id` - ID магазина
- `name` - Название магазина
- `delivery_types` - JSON с типами доставки
- `created_at` - Дата создания

#### Categories (Категории)
- `id` - ID категории
- `name` - Название категории
- `icon_url` - URL иконки
- `color` - Цвет категории
- `product_count` - Количество товаров
- `created_at` - Дата создания

#### ProductList (Товары)
- `id` - ID записи
- `item_id` - Уникальный ID товара
- `shop_id` - ID магазина
- `category_id` - ID категории
- `name` - Название товара
- `category` - Название категории (для совместимости)
- `price` - Цена
- `original_price` - Оригинальная цена
- `image_url` - URL изображения
- `description` - Описание
- `rating` - Рейтинг
- `reviews_count` - Количество отзывов
- `in_stock` - В наличии
- `discount` - Скидка в процентах
- `tags` - JSON с тегами
- `created_at` - Дата создания
- `updated_at` - Дата обновления

#### Users (Пользователи)
- `id` - ID пользователя
- `name` - Имя
- `phone` - Телефон
- `email` - Email
- `password_hash` - Хеш пароля
- `address` - Адрес
- `avatar_url` - URL аватара
- `is_active` - Активен ли аккаунт
- `created_at` - Дата создания
- `updated_at` - Дата обновления

#### UserTokens (Токены пользователей)
- `id` - ID токена
- `user_id` - ID пользователя
- `token` - Токен
- `expires_at` - Дата истечения
- `created_at` - Дата создания

## 🔧 Управление данными

### Создание таблиц
```bash
python creating_tables.py
```

### Заполнение тестовыми данными
```bash
python seed_data.py
```

### Полная инициализация
```bash
python run_server.py
```
(автоматически создаст таблицы и заполнит данными при первом запуске)

## 🧪 Тестовые данные

После запуска `seed_data.py` будут созданы:

### Тестовый пользователь
- **Телефон:** +7 900 123 45 67
- **Email:** test@example.com
- **Пароль:** password123

### Категории
- Молочные продукты
- Мясо и птица
- Овощи и фрукты
- Хлеб и выпечка
- Напитки
- Сладости
- Замороженные
- Консервы

### Магазины
- ВкусВилл
- Лента
- О'кей
- Магнит
- Перекрёсток

### Товары
Более 20 товаров в разных категориях с полной информацией.

## 🔒 Безопасность

- Пароли хешируются с помощью SHA-256
- Токены генерируются с помощью `secrets.token_urlsafe()`
- Токены действительны 30 дней
- Валидация входных данных
- CORS настроен для мобильного приложения

## 🐛 Отладка

### Логи
Сервер выводит подробные логи всех запросов в debug режиме.

### Проверка базы данных
```bash
sqlite3 main.db
.tables
.schema
```

### Тестирование API
```bash
# Проверка здоровья
curl http://localhost:5000/api/health

# Поиск товаров
curl "http://localhost:5000/api/items/search?name=молоко"

# Получение категорий
curl http://localhost:5000/api/categories
```

## 📱 Интеграция с мобильным приложением

1. Запустите сервер: `python run_server.py`
2. В мобильном приложении URL уже настроен на `http://10.0.2.2:5000/api/`
3. Для реального устройства замените URL на IP адрес компьютера

## 🚀 Развертывание

### Локально
```bash
python run_server.py
```

### В продакшене
1. Установите gunicorn: `pip install gunicorn`
2. Запустите: `gunicorn -w 4 -b 0.0.0.0:5000 app:app`
3. Настройте nginx для проксирования запросов
4. Используйте PostgreSQL вместо SQLite для продакшена
